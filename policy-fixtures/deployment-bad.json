{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": { "name": "demo-bad" },
  "spec": {
    "selector": { "matchLabels": { "app": "demo" } },
    "template": {
      "metadata": { "labels": { "app": "demo" } },
      "spec": {
        "containers": [
          {
            "name": "web",
            "image": "nginx:latest"
          }
        ]
      }
    }
  }
}
